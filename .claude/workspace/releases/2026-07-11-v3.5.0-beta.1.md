# 发布检查报告

> 版本：v3.5.0-beta.1
> 路径：.claude/workspace/releases/2026-07-11-v3.5.0-beta.1.md

## 发布结论：blocked

所有本地发布门禁通过，Git 远端可访问；npm registry 账号未登录（`npm whoami` 返回 E401），因此尚未创建 tag、推送 tag 或发布 npm 包。

## 关联任务

- 任务：N1-N7 / legacy AI preset update compatibility
- 版本：v3.5.0-beta.1
- 发布范围：`create-claude-team`、Claude/Codex workflow、规划/交付/变更/运行准备度 CLI 与文档

## 发布范围

- 分支：`main`
- tag：未创建，等待 npm 认证恢复后创建 `v3.5.0-beta.1`
- Package：`create-claude-team@3.5.0-beta.1`
- 关键产物：N1-N7 contracts、`operations validate`、旧 `ai-knowledge-base` 升级兼容、README/USAGE/CHANGELOG
- Review reports：`.claude/workspace/reviews/2026-07-11-n1-dogfood.md` 至 `2026-07-11-n7-operational-readiness.md`

## 检查结果

| 检查维度 | 结果 | 详情 |
|---|---|---|
| review | pass | N1-N7 均有审查证据；本次兼容修复由 smoke 回归覆盖 |
| test | pass | `npm run gate`：683 项通过 |
| typecheck | not_applicable | 当前包为纯 Node.js 脚本，无 TypeScript/typecheck 脚本 |
| lint | not_applicable | 当前包无 lint 脚本 |
| security | pass | hooks 行为测试、Readiness Brief 和秘密模式拒绝场景通过 |
| dependency audit | not_applicable | 无 dependencies/devDependencies；无 lockfile 时 `npm audit` 返回 ENOLOCK |
| performance | pass | Workbench `--check` 通过；无新增运行时服务 |
| accessibility | not_applicable | 本次无面向用户 UI 改动 |
| config | pass | preset manifest、Claude/Codex 同步、pack 清单和 tarball 安装通过 |
| git | pending | 等待版本提交与 push |
| rollback | pass | 可回滚到 `ac20a33`；无需数据回滚 |
| npm publish | blocked | npm registry 身份验证 E401 |

## 风险

| 风险 | 影响 | 处置 | 确认人 | 证据/后续 |
|---|---|---|---|---|
| N8 尚未完成真实项目验证 | beta 可能仍有流程体验问题 | defer | Owner | 使用 `next` 标签 dogfood，反馈后进入 N8 |
| npm 未登录 | 当前无法发布 registry 包 | mitigate | Owner | 执行 `npm login` 后重试发布 |

## 回滚步骤

- 发布前：不创建 tag；如 Git 提交已推送，可 revert 该版本提交。
- npm 发布后：不覆盖版本；发布修复 beta 或 stable 版本，并视影响 deprecate 有问题版本。
- 回滚触发条件：安装失败、`init/update/validate` 失败、旧 AI preset 升级丢失专用规则、发布后核心命令不可用。
- 数据回滚：无数据库、无运行数据变更。

## 发布后验证

- `npx create-claude-team@next init --preset ai-app --dry-run`
- 旧项目 `.preset` 为 `ai-knowledge-base` 时执行 `update --dry-run`，确认保留 AI skills。
- `npx create-claude-team@next operations validate <brief>`
- 观察 npm 包页、GitHub Actions 与首个真实项目的开发/修复/发布流程。

## Gate 结果

- release：blocked
- warnings：1（N8 dogfood 延后，已按 beta/next 处置）
- blockers：1（npm authentication）
- approver：Owner

## Summary

- status: blocked
- affected files/modules: create-claude-team@3.5.0-beta.1, .claude/workspace/releases/2026-07-11-v3.5.0-beta.1.md
- checks: review pass / gate pass / tarball pass / operations pass / npm publish blocked
- next action: 完成 npm login 后创建 tag 并发布 `next`

## Next Best Action

- action: 执行 npm login 后重试 npm 发布
- reason: 所有本地门禁通过，唯一阻塞是 npm registry E401
- requires human confirmation: yes
- source: npm whoami / local gate / tarball smoke / this release report
