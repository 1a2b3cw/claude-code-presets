# 跨文件审查报告：M2 Planning Upgrade

> Lifecycle: reference
> 审查日期：2026-07-10

## 结论

**pass**：M2.1 和 M2.2 的 Product Lead 规划契约、Product Brief 边界与 Claude/Codex 路径同步一致，无阻塞问题。

## 关联任务

- M2.1 升级 `/plan` 为 Product Lead 主导
- M2.2 强化 Product Brief 和 Project Preset 边界

## 变更范围

- `.claude/commands/plan.md` 与 Codex command/skill 镜像
- `.claude/commands/project-preset.md` 与 Codex command/skill 镜像
- `create-claude-team/lib/codex/text.js`
- `create-claude-team/scripts/smoke-test.js`
- `docs/m2-planning-upgrade-spec.md`、`docs/maturity-tasks.md`

## 审查结果

- Product Lead、Architect-Planner 与 Delivery Steward 的职责交接和 `/plan` 输出字段一致。
- roadmap 模板新增用户价值与 MVP 归属，推荐顺序由 Product Lead 提供。
- `project-profile/product.md` 只保留事实、已确认决策、推断和未决问题；未确认推断不能进入 Product Brief 或 preset 规则。
- `project-preset/rules/` 是唯一跨 Claude/Codex 可移植的规则目录；目录树展示和运行时路径均一致。
- 修复了 Codex 文本转换器会把 project-preset 目录树中的相对 `rules/` 与 `specs/` 错改为 `.agents/` 的问题，并增加回归测试。

## 问题列表

- critical：0
- major：0
- minor：0
- suggestion：0

## 自动修复项

- 第 1 轮：修复 `project-preset/` 目录树被 Codex 路径转换误改的问题，并添加目录树回归断言。

## 检查结果

- `npm run validate`：pass
- `npm test`：pass（524 passed / 0 failed）
- `git diff --check`：pass

## Artifact Cleanup

- Kept as source of truth: `docs/maturity-tasks.md`、`docs/m2-planning-upgrade-spec.md`
- Merged into: 无
- Archived: 无
- Deleted: 无
- Decisions preserved: Owner 已确认 `project-preset/rules/` 为唯一规则目录。
- Follow-up: 根目录 `tasks.md` 仍是旧迭代状态源，应在后续 cleanup 中合并或归档，不在本任务删除。

## Gate 结果

- review: pass
- critical: 0
- major: 0
- minor: 0
- fix rounds: 1

## 下一步

执行 M3.1，收敛 `/dev` 的写入面。
